class Main {
    function void main() {
        var Game game;
        var Theme theme;
        let game = Game.new();
        do game.run();
        do game.dispose();
        let theme = Theme.new();  
        do theme.run();
        do theme.dispose();
        return;
    }
}
